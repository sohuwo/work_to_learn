#include "p24t8.h"
#include <stdio.h>

void p24t8(void)
{
	float loan = 0, interestRate =0,monthlyPayment = 0;
	printf("Enter amount of loan: ");
	scanf("%f", &loan);
	printf("Enter interest rate:");
	scanf("%f", &interestRate);
	printf("Enter monthly payment:");
	scanf("%f", &interestRate);

	while (loan >= 0)
	{

	}

}